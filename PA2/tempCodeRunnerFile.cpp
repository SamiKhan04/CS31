       // else if (overstated_amt <= TIER2) {
       //        if (location == "florida" || location == "new york") {
       //               fine = fine + TIER2 * FINE3;
       //               overstated_amt = 0;
       //        }
       //        else {
       //               fine = fine + TIER2 * FINE2;
       //               overstated_amt = 0;
       //        }
       // }